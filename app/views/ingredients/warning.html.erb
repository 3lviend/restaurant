<div class="row">
  <div class="col-lg-12">
    <h2 class="page-header">Ingredients Warning</h2>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
        <tr>
          <th>Name</th>
          <th>Quantity</th>
          <th>Detail Of Quantity</th>
        </tr>
      </thead>
      <tbody>
        <% @ingredient.each do |i| %>
        <tr>
          <td><%= i.name %></td>
          <td><%= i.quantity %></td>
          <td><%= i.detail_of_quantity %></td>
          <!-- <td><%#= link_to 'Edit', edit_ingredients_path(i) %></td>
          <td><%#= link_to 'Delete', method: :delete, data:{confirm: 'Are you sure?'}, class: 'btn btn-warning' %></td> -->
        </tr>
        <% end %>
      </tbody>
    </table>
    <%#= link_to 'ADD', new_waitress_path, class:'btn btn-default' %>
  </div>
</div>